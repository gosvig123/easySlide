{
  "devDependencies": {
    "turbo": "^1.6.3"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "dev": "dotenv -- turbo run dev"
  },
  "private": true,
  "dependencies": {
    "dotenv-cli": "^6.0.0"
  }
}
