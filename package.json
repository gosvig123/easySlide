{
  "devDependencies": {
    "turbo": "^1.6.3"
  },
  "workspaces": [
    "client/*",
    "server/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "dev": "turbo run dev"
  }
}
