{
  "devDependencies": {
    "turbo": "^1.6.3"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "test:setup": "turbo run test:setup",
    "dev": "docker-compose up -d && dotenv -- turbo run dev",
    "dev:setup": "docker-compose up -d && turbo run dev:setup && docker-compose down"
  },
  "private": true,
  "dependencies": {
    "dotenv-cli": "^6.0.0",
    "node-fetch": "^3.3.0"
  }
}
